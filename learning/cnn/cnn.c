int main()
{
	cnn_malloc();
	cnn_malloc();
	cnn_memset();
	cnn_memset();

}


create_datatype()
{
	// function to allocate memory for data type
	sbrk();	
}

void cnn_memset()
{
	if()	{
		// record info in 
		mem[i-5] = info;
		size+=num*sizeof(var);
		mem[	
		sblock = 100;
		size = 1000;
	} else if()	{
		sbrk(size);		
	}
}

(void *)cnn_malloc()
{
	cnn_memset();
}


#define inf	do{sbrk(2^1267650600228229401496703205376);}while(0)

void sum()
{
	while(1)	{
		from 0 to inf
				
	}
}

void cnn_math()
{
	delta = delta*sigma;
	sum();
}
