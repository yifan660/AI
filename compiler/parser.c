void rule()
{
	if(character is single)	{
		check context of character
	} else if( is number)	{
		check rule
	} else if()	{

	} else if()	{

	} else if()	{

	} else if()	{

	} else if()	{

	}
}

recursion()
{
	
}

void opcode()
{
	if(op == 'a')	{
		check again for next character 	
	} else if(op == 'b')	{
		check again for next character
	} else if(op == 'c')	{
		check again for next character
	} else if(op == 'd')	{
		check again for next character
	} else if(op == 'e')	{
		check again for next character
	} else if(op == 'f')	{
		check again for next character
	} else if(op == 'g')	{
		check again for next character
	} else if(op == 'h')	{
		check again for next character
	} else if(op == 'i')	{
		check again for next character
	} else if(op == 'j')	{
		check again for next character
	} else if(op == 'k')	{
		check again for next character
	} else if(op == 'l')	{
		check again for next character
	} else if(op == 'm')	{
		check again for next character
	} else if(op == 'n')	{
		check again for next character
	} else if(op == 'o')	{
		check again for next character
	} else if(op == 'p')	{
		check again for next character
	} else if(op == 'q')	{
		check again for next character
	} else if(op == 'r')	{
		check again for next character 
	} else if(op == 's')	{
		check again for next character
	} else if(op == 't')	{
		check again for next character
	} else if(op == 'u')	{
		check again for next character
	} else if(op == 'v')	{
		check again for next character
	} else if(op == 'w')	{
		check again for next character
	} else if(op == 'x')	{
		check again for next character
	} else if(op == 'y')	{
		check again for next character
	} else if(op == 'z')	{
		check again for next character
	}
	
	loop
}


int main()
{
	int a;
	int b;

	for()	{	
		

	}

	return 0;
}
